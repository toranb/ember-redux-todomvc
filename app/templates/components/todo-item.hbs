{{yield
  (action "deleteTodo" todo.id)
  (action "completeTodo" todo.id)
  (action "editTodo" todo.id)
  (action "handleKeydown")
  (action "focusInput")
  (action "startEditing")
  (action "doneEditing")
}}
